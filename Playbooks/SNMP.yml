
- hosts: Router1
  gather_facts: false
  tasks:
    - name: Router SNMP configuration
      cisco.ios.ios_snmp_server:
        running_config:
          communities:
              name: ahmed
              rw: true