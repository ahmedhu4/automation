
- hosts: Router1
  gather_facts: false
  tasks:
    - name: Router SNMP configuration
      cisco.ios.ios_snmp_server:
        config:
          communities:
            - acl_v4:
              name: ahmed
              rw: true
